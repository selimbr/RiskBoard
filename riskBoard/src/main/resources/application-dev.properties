# Activé uniquement via --spring.profiles.active=dev (ou SPRING_PROFILES_ACTIVE=dev).
# Jamais actif par défaut, donc jamais exposé dans l'image Docker de prod.
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
